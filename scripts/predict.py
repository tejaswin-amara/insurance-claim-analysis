import joblib\nimport pandas as pd\nimport sys\n\ndef predict_charge(age, bmi, children, smoker):\n    # Load model\n    try:\n        model = joblib.load('insurance_model.pkl')\n    except:\n        model = joblib.load('scripts/insurance_model.pkl')\n    \n    # Prepare input\n    smoker_val = 1 if smoker.lower() == 'yes' else 0\n    interaction = bmi * smoker_val\n    \n    # Feature names must match training\n    data = {\n        'age': [age],\n        'bmi': [bmi],\n        'children': [children],\n        'bmi_smoker_interaction': [interaction],\n        'smoker_yes': [smoker_val]\n    }\n    \n    df_input = pd.DataFrame(data)\n    prediction = model.predict(df_input)[0]\n    return max(0, prediction)\n\nif __name__ == \"__main__\":\n    if len(sys.argv) < 5:\n        print(\"Usage: python predict.py <age> <bmi> <children> <smoker_yes/no>\")\n    else:\n        age = float(sys.argv[1])\n        bmi = float(sys.argv[2])\n        children = int(sys.argv[3])\n        smoker = sys.argv[4]\n        res = predict_charge(age, bmi, children, smoker)\n        print(f\"Predicted Insurance Charge: ${res:,.2f}\")\n
